# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Env / secrets
.env

# Data: raw transcripts are publisher copyright — never commit.
# Derived outputs ARE committed (the portfolio artifact); drafts are intermediate.
data/transcripts/*.txt
data/transcripts/*.json
data/output/*.draft.json
# v2 research records are generated snapshots (prices vary daily) — regenerate via
# scripts/research.py --json; the curated qualitative cache (data/qualitative/) IS committed.
data/output/*.research.json

# Transient operational state (periodic refresh poll timestamps/accessions).
data/refresh_state.json

# OS / editor
.DS_Store
.idea/
.vscode/
